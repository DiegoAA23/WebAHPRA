<div class="event-section">
  {{#each events}}
    <div class="event">
      <h2>{{name}}</h2>
      <p><strong>Fecha:</strong> {{date}}</p>
      <p><strong>Lugar:</strong> {{location}}</p>
      <p>{{description}}</p>
    </div>
  {{/each}}
</div>